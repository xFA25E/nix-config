#!/bin/sh
[ "$#" -lt 2 ] && exit 1

dir="${1:?provide directory}"
mkdir -p "${dir}" || exit 1

shift
for video in "$@"; do
    ffmpeg -n -i "${video}" -vcodec libx264 -crf 23 "${dir%/}/${video%.*}.mkv"
done
