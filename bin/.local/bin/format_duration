#!/bin/sh

awk -v pos="${1:-1}" '
{
    $pos = sprintf("%d:%d:%02d:%02d:%02d", $pos / (60 * 60 * 24 * 356), ($pos / (60 * 60 * 24)) % 356, ($pos / (60 * 60)) % 24, ($pos / 60) % 60, $pos % 60)
    sub("^(0+:)+", "", $pos)
    print $0
}
'
